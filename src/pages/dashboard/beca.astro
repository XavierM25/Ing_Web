---
import DashboardLayout from '../../layouts/DashboardLayout.astro';

const userData = {
  nombre: 'David Montaño',
  rol: 'Estudiante',
  cursos: 12,
  certificados: 5,
};
import '../../layouts/styles/global.css';
---

<DashboardLayout title="Postulación a Beca">
  <div
    class="min-h-screen bg-gradient-to-br bg-white py-12 px-4 sm:px-6 lg:px-8"
  >
    <div class="max-w-3xl mx-auto">
      <div class="text-center mb-12">
        <h1 class="font-onest text-4xl font-bold text-[#222] mb-4">
          Postula a una Beca Gratuita
        </h1>
        <p class="text-lg text-gray-600">
          Impulsa tu carrera profesional con nuestras becas de estudio
        </p>
      </div>

      <form class="bg-white rounded-xl shadow-lg p-8">
        <div class="space-y-6">
          <div>
            <label
              for="nombre"
              class="block text-sm font-medium text-gray-700 mb-2"
            >
              Nombre completo
            </label>
            <input
              type="text"
              id="nombre"
              name="nombre"
              required
              class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 transition-colors"
              placeholder="Ingresa tu nombre completo"
            />
          </div>

          <div>
            <label
              for="email"
              class="block text-sm font-medium text-gray-700 mb-2"
            >
              Correo electrónico
            </label>
            <input
              type="email"
              id="email"
              name="email"
              required
              class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 transition-colors"
              placeholder="tu@email.com"
            />
          </div>

          <div>
            <label
              for="curso"
              class="block text-sm font-medium text-gray-700 mb-2"
            >
              Curso de interés
            </label>
            <select
              id="curso"
              name="curso"
              required
              class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 transition-colors"
            >
              <option value="">Selecciona un curso</option>
              <option value="web">Desarrollo Web</option>
              <option value="mobile">Desarrollo Mobile</option>
              <option value="data">Ciencia de Datos</option>
            </select>
          </div>

          <label
            for="motivacion"
            class="block text-sm font-medium text-gray-700 mb-2"
          >
            ¿Por qué deberías recibir la beca?
          </label>
          <textarea
            id="motivacion"
            name="motivacion"
            required
            rows="4"
            class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 transition-colors"
            placeholder="Cuéntanos por qué te gustaría recibir esta beca..."
          ></textarea>
        </div>

        <button
          type="submit"
          class="w-full bg-indigo-600 text-white py-3 px-6 rounded-lg font-medium hover:bg-indigo-700 transform hover:scale-[1.02] transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500"
        >
          Enviar postulación
        </button>
      </form>
    </div>

    <p class="text-center text-gray-600 mt-6">
      Al enviar este formulario, aceptas nuestros términos y condiciones
    </p>
  </div>
</DashboardLayout>
