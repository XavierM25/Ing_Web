---
import Layout from "../layouts/Layout.astro";
import Login from "../components/auth-components/login.astro";
import Register from "../components/auth-components/register.astro";
import {Button} from '@nextui-org/react';
---

<Layout title="Guimarbot: Autenticación">
    <main class="mx-auto w-1/4 mt-20 shadow-lg animate-fade-in-dow">
        <div class="border-[1px] rounded-2xl p-5 bg-black text-white">
            <div class="w-full flex flex-row" style="font-family: Inter, sans-serif;">
                <Button id="btnLogin" radius ="none" className="bg-white bg-opacity-5 text-white border-[#3f3f3f] border-y-[1px] border-l-[1px] p-2 rounded-s-2xl w-full" client:visible>Iniciar sesión</Button>
                <Button id="btnRegister" radius ="none" className="bg-white bg-opacity-5 text-white border-[#3f3f3f] border-[1px] p-2 rounded-e-2xl w-full" client:visible>Registrarse</Button>
            </div>
            <div id="loginContainer" class="w-full">
                <Login />
            </div>
            <div id="registerContainer" class="hidden w-full">
                <Register />
            </div>
        </div>
    </main> 
</Layout>

<script>
    const loginContainer = document.getElementById('loginContainer');
    const registerContainer = document.getElementById('registerContainer');

    const btnLogin = document.getElementById('btnLogin');
    const btnRegister = document.getElementById('btnRegister');

    btnLogin?.addEventListener('click', () => {
        loginContainer?.classList.remove('hidden');
        registerContainer?.classList.add('hidden');
    });

    btnRegister?.addEventListener('click', () => {
        registerContainer?.classList.remove('hidden');
        loginContainer?.classList.add('hidden');
    });
</script>
